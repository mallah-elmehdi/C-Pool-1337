#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char	*ft_strstr(char *str, char*to_find);

int main()
{
    char tab1[] = "dsf df aab dfsd";
    char tab2[] = "ab";
    printf("%s\n", ft_strstr(tab1, tab2));
   // printf("%s", strstr(tab1, tab2));
    

    return 0;
}
